[package]
name = "p10k-rs-git"
description = "Git status engine for p10k-rs: gix for index/refs, rustix for the hot walker."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
p10k-rs-core = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
# `gix`, `rustix`, `rayon` were dependencies of the pre-pivot in-process
# scanner architecture (ADR-0001). They'll come back when the gitstatusd
# client + `gix-status` fallback land in slice 5+; until then they'd just
# be unused-dep noise.

[lints]
workspace = true
