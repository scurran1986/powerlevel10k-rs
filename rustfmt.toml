# Stable-only rustfmt knobs. Anything `unstable_features = true` is off-limits because
# CI runs stable rustfmt. Defaults are fine for the rest.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
